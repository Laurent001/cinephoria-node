{
  "name": "backend",
  "version": "1.0.0",
  "main": "build/server.js",
  "scripts": {
    "test": "mocha tests/**/*.test.js",
    "build": "mkdir -p build/src build/db && cp -r src/* build/src/ && cp -r db/* build/db/",
    "start": "node build/server.js",
    "dev": "nodemon src/server.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mariadb": "^3.4.0",
    "nodemailer": "^6.10.0",
    "validator": "^13.12.0"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.0.0",
    "nodemon": "^2.0.15"
  }
}
